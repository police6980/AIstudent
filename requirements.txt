anthropic>=0.40.0
openai>=1.50.0
elevenlabs>=1.0.0
# Gradio 5.x ships compatible versions of gradio_client, huggingface_hub,
# starlette, fastapi, and jinja2 in its own pin set. Staying on 4.x
# required chasing four separate upstream regressions (HfFolder removed,
# Pydantic bool schemas, starlette 0.40 API break, jinja2 cache dict).
gradio>=5.0,<6.0
sqlalchemy>=2.0
pydantic>=2.0
pyyaml>=6.0
python-dotenv>=1.0.0
reportlab>=4.0
pypdf>=4.0
networkx>=3.0
matplotlib>=3.7
