anthropic>=0.40.0
openai>=1.50.0
elevenlabs>=1.0.0
# Pin gradio below 5 and huggingface_hub below 1.0 — gradio 4.44.0's
# internal `from huggingface_hub import HfFolder` breaks once hub reaches 1.0
# (HfFolder was removed). Keep these pins until we upgrade to gradio 5.x.
gradio>=4.44.0,<5.0
huggingface_hub>=0.24,<1.0
sqlalchemy>=2.0
pydantic>=2.0
pyyaml>=6.0
python-dotenv>=1.0.0
reportlab>=4.0
pypdf>=4.0
networkx>=3.0
matplotlib>=3.7
