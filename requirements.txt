anthropic>=0.40.0
openai>=1.50.0
elevenlabs>=1.0.0
# Pin gradio in the 4.44.1 — 4.x range:
# - 4.44.0 ships a gradio_client bug that crashes on Pydantic-derived
#   schemas ("TypeError: argument of type 'bool' is not iterable");
#   4.44.1 fixes it.
# - gradio 5.x has breaking changes we haven't migrated to yet.
# huggingface_hub 1.0 removed HfFolder which gradio 4.x still imports.
gradio>=4.44.1,<5.0
huggingface_hub>=0.24,<1.0
sqlalchemy>=2.0
pydantic>=2.0
pyyaml>=6.0
python-dotenv>=1.0.0
reportlab>=4.0
pypdf>=4.0
networkx>=3.0
matplotlib>=3.7
