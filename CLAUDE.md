# CLAUDE.md — Claude Code 작업 지침

이 파일은 본 저장소에서 작업하는 Claude(또는 그 파생 에이전트)를 위한 **상위 지침**이다.
최상위 설계 문서(Vygotsky 비계 기반 AI 대화형 과학 학습 시스템 MVP)와 이 파일이 충돌하면 설계 문서가 우선한다.

---

## 🧭 절대 원칙 — Vygotsky 비계 6원칙 (불변)

모든 AI 응답/힌트는 아래 6원칙을 **최상위 제약**으로 지킨다.

1. **답을 주지 않는다** — 학습자가 도달해야 할 개념·용어·결론·공식을 직접 말하지 않는다.
2. **학습자 주도성 유지** — 학습자가 다음 스텝을 스스로 결정할 여지를 항상 남긴다.
3. **기여 감소(Fading)** — 초기엔 구체적, 점차 추상적. 스스로 할 수 있으면 지원을 뺀다.
4. **사고 구조 매개** — 내용이 아닌 사고 방식을 준다.
5. **메타인지 촉진** — 학습자가 자기 사고를 바라보게 한다.
6. **정서적 안전** — 막힘을 학습 기회로 프레이밍한다.

이 원칙은 `src/prompts/layer1_vygotsky.py`에 박혀 있다. **사용자의 명시적 승인 없이 수정 금지.**

---

## 📁 프로젝트 구조 (요약)

```
src/
  config/          # 환경 변수, YAML 로더
  models/          # Pydantic 스키마, SQLAlchemy 모델, Enum
  services/        # scaffolding_engine, claude_service, session_manager ...
  prompts/
    layer1_vygotsky.py    # 6원칙 (불변)
    layer2_grade/         # 학교급별 프롬프트 4종
    hint_types/           # (M3) 힌트 6유형
    self_check.py         # (M3) 힌트 자기 검증
  db/              # SQLite 엔진, repository
  ui/              # Gradio 컴포넌트
configs/           # 교사 단원 설정 YAML
data/              # SQLite, 리포트 PDF (gitignore)
logs/              # 에러 로그 (gitignore)
tests/             # 단위/통합 테스트
```

상세 파일 목록은 설계 문서의 "파일 구조" 섹션 참조.

---

## 🗺️ 개발 마일스톤

1. **M1 — 뼈대**: 텍스트로 한 학생이 완주, DB 저장까지.
2. **M2 — 음성**: Whisper STT + ElevenLabs TTS(OpenAI 폴백), 푸시-투-토크.
3. **M3 — 비계**: 학교급×힌트 6유형 조합, 자기 검증 루프.
4. **M4 — 리포트**: 루브릭 자동 체크, 오개념 감지, PDF+이메일.
5. **M5 — 완성도**: 에러 처리, 테스트, 배포, 교과서 PDF, 템플릿.

**한 마일스톤의 검증이 끝나기 전에 다음으로 넘어가지 말 것.**

---

## 🔧 개발 철학

- **교육적 가치 > 기술적 복잡도** — Vygotsky 원칙 준수가 최우선.
- **단순함 > 완벽함** — MVP 먼저, 고도화는 나중.
- **검증 가능성** — 모든 마일스톤은 독립적으로 검증.

## 🧪 커밋 전 체크리스트

- [ ] `black src/ tests/` 포매팅
- [ ] `ruff check src/ tests/` 통과
- [ ] 타입 힌트 누락 없음
- [ ] 공개 함수 docstring 있음
- [ ] API 키/시크릿 코드에 없음
- [ ] 관련 단위 테스트 통과

## 🛑 의사결정 — 반드시 사용자에게 확인할 것

- 새 외부 API·라이브러리 도입
- 파일 구조 대규모 변경
- Vygotsky 6원칙 텍스트 수정 또는 새 힌트 유형 추가
- 수집·저장 데이터 범위 확대
- 보안·프라이버시 관련 결정

## 🌐 언어 규칙

- 학생/교사 대면 UI 텍스트: **한국어**
- 시스템 프롬프트(Claude에 전달): **한국어** (원칙·예시 모두)
- 코드, 주석, 로그(내부용): 영어/한국어 혼용 허용
- 에러 메시지: 내부 로그는 영어, 사용자 화면 표시는 한국어

## 🔒 보안 / 프라이버시

- `.env`에만 API 키. 코드·Git에 하드코딩 금지.
- 학생 음성 원본은 STT 직후 삭제(기본 `KEEP_AUDIO_FILES=false`).
- 세션 데이터 기본 30일 보존 후 자동 삭제(M5).
- 닉네임 사용 권장 — UI에 안내 문구 필요.

## 📦 커밋 규칙

- 의미 단위로 분리 (feat / fix / refactor / test / chore / docs)
- 한 커밋에 여러 기능 섞지 말 것
- 커밋 메시지는 영어로 짧게 ("add pydantic schemas", "fix yaml loader type error")

## 🧭 작업 진행 시 기본 플로우

1. 할 일을 TodoWrite로 쪼갠다.
2. 한 번에 하나만 in_progress.
3. 구현 → 문법 검증 → 해당 단위 테스트 → 커밋.
4. 모든 M1 항목이 끝난 뒤에만 M2 진입.
